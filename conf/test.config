// conf/test.config
// Quick-running slice that still has loci for T1 (avoid chr21).
// :contentReference[oaicite:8]{index=8}

params {
  chroms = ['6']
  outdir = "${params.output_path}_test"
}
process {
  maxForks = 1
}
