// conf/local.config
// Local dev tweaks; still assumes `conda activate postgwas_py39` before running.
// :contentReference[oaicite:6]{index=6}

process {
  cpus   = 2
  memory = '6 GB'
  withLabel: 'heavy' { cpus = 8; memory = '14 GB' }
}
params {
  outdir = "${params.output_path}_local"
}
